name: "APAS Audit"
description: "Audit AI system prompts against the APAS (AI Prompt Assurance Standard) in your CI/CD pipeline"
author: "Xiangning Lin"

branding:
  icon: "shield"
  color: "blue"

inputs:
  prompts-dir:
    description: "Path to the directory containing system prompt files"
    required: true
    default: "./prompts"
  threshold:
    description: "Minimum compliance rate (0-100) to pass the audit"
    required: false
    default: "0"
  api-url:
    description: "Base URL of the APAS auditing service"
    required: false
    default: "https://prompt-auditing-service.vercel.app"
  api-key:
    description: "API key for the APAS service"
    required: false
  model:
    description: "LLM model to use for auditing"
    required: false
  output-format:
    description: "Output format: table or json"
    required: false
    default: "table"
  version:
    description: "Version of apas-audit CLI to install"
    required: false
    default: "latest"

outputs:
  result:
    description: "JSON audit result"
    value: ${{ steps.audit.outputs.result }}
  status:
    description: "Overall status: PASSED or FAILED"
    value: ${{ steps.audit.outputs.status }}

runs:
  using: "composite"
  steps:
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install apas-audit
      shell: bash
      run: |
        if [ "${{ inputs.version }}" = "latest" ]; then
          pip install apas-audit
        else
          pip install "apas-audit==${{ inputs.version }}"
        fi

    - name: Run APAS audit
      id: audit
      shell: bash
      env:
        APAS_API_URL: ${{ inputs.api-url }}
        APAS_API_KEY: ${{ inputs.api-key }}
        APAS_MODEL: ${{ inputs.model }}
      run: |
        set +e

        ARGS="${{ inputs.prompts-dir }}"
        ARGS="$ARGS --threshold ${{ inputs.threshold }}"
        ARGS="$ARGS --output ${{ inputs.output-format }}"

        if [ "${{ inputs.output-format }}" = "json" ]; then
          RESULT=$(apas-audit $ARGS 2>&1)
          EXIT_CODE=$?
          echo "result<<EOF" >> $GITHUB_OUTPUT
          echo "$RESULT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        else
          apas-audit $ARGS
          EXIT_CODE=$?
        fi

        if [ $EXIT_CODE -eq 0 ]; then
          echo "status=PASSED" >> $GITHUB_OUTPUT
        else
          echo "status=FAILED" >> $GITHUB_OUTPUT
        fi

        exit $EXIT_CODE
